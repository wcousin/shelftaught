[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[phases.start]
cmd = 'npx serve -s dist -l $PORT'

[variables]
NODE_ENV = 'production'
PORT = '3000'